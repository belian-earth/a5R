% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{a5_cell_child}
\alias{a5_cell_child}
\title{The i-th child of each cell}
\usage{
a5_cell_child(cell, resolution, i)
}
\arguments{
\item{cell}{An \link{a5_cell} vector.}

\item{resolution}{Integer scalar target resolution, at or finer than every
cell's own resolution.}

\item{i}{Integer vector of 1-based child positions, recycled against
\code{cell}. Must lie within \code{1:a5_get_num_children(res, resolution)} for each
cell's resolution \code{res}; out of range is an error.}
}
\value{
An \link{a5_cell} vector the same length as \code{cell}. \code{NA} where \code{cell}
or \code{i} is \code{NA}.
}
\description{
Returns one descendant of each cell at a finer resolution without
enumerating the others: the \code{i}-th element of what
\code{\link[=a5_cell_to_children]{a5_cell_to_children()}} would return. Useful for random draws from large
cells, where building the full child list to pick one element is wasteful.
}
\examples{
cell <- a5_lonlat_to_cell(-3.19, 55.95, resolution = 5)
a5_cell_child(cell, resolution = 7, i = 1:16)
identical(a5_cell_child(cell, 7, 1:16), a5_cell_to_children(cell, 7))
}
\seealso{
\code{\link[=a5_cell_to_children]{a5_cell_to_children()}}, \code{\link[=a5_get_num_children]{a5_get_num_children()}}
}
