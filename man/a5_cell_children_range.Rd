% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{a5_cell_children_range}
\alias{a5_cell_children_range}
\title{Range of descendant cell ids}
\usage{
a5_cell_children_range(cell, resolution)
}
\arguments{
\item{cell}{An \link{a5_cell} vector.}

\item{resolution}{Integer scalar target resolution from 0 to 29, at or
finer than every cell's own resolution.}
}
\value{
A data frame with \link{a5_cell} columns \code{lo} and \code{hi}, one row per
input cell. \code{NA} where \code{cell} is \code{NA}.
}
\description{
Returns the smallest and largest descendant of each cell at a finer
resolution, without enumerating the descendants. Because the A5 index
encodes position along a Hilbert curve with a cell's descendants sharing
its leading bits, the descendants of a cell at any resolution up to 29
occupy a contiguous range of ids among all cells at that resolution: every
cell at \code{resolution} whose id lies between \code{lo} and \code{hi} inclusive is a
descendant, and no descendant lies outside. This makes \verb{BETWEEN lo AND hi}
an exact filter on a store of \code{resolution} cells sorted by id. Not every
integer in the range is a valid cell, and cells of other resolutions may
fall inside it.
}
\details{
Resolution 30 is refused because the guarantee does not hold there. The
index has no room for the usual face bits at that resolution, so faces use
three different bit layouts whose id ranges overlap numerically, and a
range can contain valid resolution-30 cells from another face. Eighteen of
the sixty faces cannot be encoded at resolution 30 at all; the upstream
library falls back to resolution 29 for them. Use
\code{\link[=a5_cell_to_children]{a5_cell_to_children()}} if you need resolution-30 descendants.
}
\examples{
cell <- a5_lonlat_to_cell(-3.19, 55.95, resolution = 5)
rng <- a5_cell_children_range(cell, resolution = 8)
children <- vctrs::vec_sort(a5_cell_to_children(cell, resolution = 8))
identical(rng$lo, children[1])
identical(rng$hi, children[length(children)])
}
\seealso{
\code{\link[=a5_cell_to_children]{a5_cell_to_children()}}, \code{\link[=a5_cell_to_arrow]{a5_cell_to_arrow()}} for exact 64-bit
ids to pass to SQL.
}
